name: Auto Sync and FTP Deploy

on:
  schedule:
    - cron: '0 0 * * *' 
  workflow_dispatch: 

jobs:
  sync-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: 🔍 Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.MY_SYNC_TOKEN }}

      - name: 🔄 Sync From Upstream
        env:
          MY_TOKEN: ${{ secrets.MY_SYNC_TOKEN }}
        run: |
          # 1. Temel Ayarlar
          git config --global --add safe.directory "$GITHUB_WORKSPACE"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
          # 2. Uzak Depoları Kontrol Et ve Temizle
          git remote remove upstream || true
          git remote add upstream https://github.com/raciplak/php-charm-studio.git
          
          # 3. Veriyi Çek
          echo "Orijinal repodan veri çekiliyor..."
          git fetch upstream master
          
          # 4. Main Dalını Güncelle
          echo "Main dalı güncelleniyor..."
          git checkout main
          git reset --hard upstream/master
          
          # 5. Push Denemesi (Hata verirse URL'yi temizle)
          echo "Kendi reponuza yükleniyor..."
          # Kullanıcı adını ve tokenı dinamik olarak ekliyoruz
          git push "https://x-access-token:${MY_TOKEN}@github.com/${{ github.repository }}.git" main --force

      - name: 🚀 FTP Deploy
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          server-dir: ./public_html/eticaret/php-charm-studio-main/
          exclude: |
            **/.git*
            **/.github*
