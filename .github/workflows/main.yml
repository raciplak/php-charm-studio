name: Auto Sync and FTP Deploy

on:
  schedule:
    - cron: '0 0 * * *' 
  workflow_dispatch: 

jobs:
  sync-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: 🔍 Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.MY_SYNC_TOKEN }}

      - name: 🔄 Sync From Upstream
        env:
          MY_TOKEN: ${{ secrets.MY_SYNC_TOKEN }}
        run: |
          # Git kimlik ayarları
          git config --global --add safe.directory "$GITHUB_WORKSPACE"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
          # Upstream bağlantısını temizle ve yeniden ekle
          git remote remove upstream || true
          git remote add upstream https://github.com/raciplak/php-charm-studio.git
          
          # Tüm dalları çek
          git fetch upstream main
          
          # Main dalına geç ve orijinal main ile eşitle
          git checkout main
          git reset --hard upstream/main
          
          # Kendi repona zorla gönder (PAT kullanarak)
          git push "https://x-access-token:${MY_TOKEN}@github.com/${{ github.repository }}.git" main --force

      - name: 🚀 FTP Deploy
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          server-dir: ./public_html/eticaret/php-charm-studio-main/
          exclude: |
            **/.git*
            **/.git/**
            **/.github/**
            **/node_modules/**
            .ftp-deploy-sync-state.json
